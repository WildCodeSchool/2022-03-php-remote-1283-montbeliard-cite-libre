{{ form_start(form) }}
<div class="d-flex flex-row justify-content-center align-items-center">
<div class='w-50'>
    {{ form_widget(form) }}
</div>

    <button class="btn btn-outline-success mt-2 ms-2">Rechercher</button>
</div>

{{ form_end(form) }}
