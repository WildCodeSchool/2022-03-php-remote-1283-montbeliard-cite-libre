{{ form_start(form) }}
    {{ form_row(form.question) }}
    {{ form_row(form.level) }}
    {% for answer in form.answers %}
        {{ form_row(answer.answer) }}
    {% endfor %}
    <button class="btn btn-outline-success mb-3"><i class="bi bi-save"></i>{{ button_label|default(' enregister') }}</button>
{{ form_end(form) }}
