<form method="post" action="{{ path('classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette classe?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
<button class="btn btn-outline-danger mt-3"><i class="bi bi-trash3"></i> supprimer</button>
</form>
